// Super-admin create user form — re-uses the admin form component.
// Supports all roles including 'admin' when the caller is super_admin.
export { default } from "@/app/admin/users/new/[role]/page"
