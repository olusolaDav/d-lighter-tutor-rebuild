/**
 * Convenience re-export so dashboard pages can import useAuth from the
 * conventional `@/lib/hooks/use-auth` path that is already referenced across
 * the codebase.
 */
export { useAuth } from '@/lib/auth/AuthContext';
